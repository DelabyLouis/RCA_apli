{% extends 'base.html.twig' %}

{% block title %}{{ mode_de_paiement.libelle }}{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1><i class="fas fa-credit-card me-2"></i>{{ mode_de_paiement.libelle }}</h1>
                <div>
                    <a href="{{ path('app_mode_de_paiement_index') }}" class="btn btn-secondary">
                        <i class="fas fa-arrow-left me-1"></i>Retour à la liste
                    </a>
                    <a href="{{ path('app_mode_de_paiement_edit', {'id': mode_de_paiement.id}) }}" class="btn btn-warning">
                        <i class="fas fa-edit me-1"></i>Modifier
                    </a>
                </div>
            </div>

            <div class="card shadow-sm">
                <div class="card-header bg-info text-white">
                    <h5 class="mb-0">
                        <i class="fas fa-info-circle me-2"></i>Informations du mode de paiement
                    </h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <table class="table table-borderless">
                                <tr>
                                    <th class="text-muted" style="width: 30%;">ID :</th>
                                    <td><span class="badge bg-primary">{{ mode_de_paiement.id }}</span></td>
                                </tr>
                                <tr>
                                    <th class="text-muted">Libellé :</th>
                                    <td><strong>{{ mode_de_paiement.libelle }}</strong></td>
                                </tr>
                            </table>
                        </div>
                        
                        <div class="col-md-6">
                            <div class="d-flex flex-column gap-3 sticky-top px-5" style="top: 20px;">
                                <h6 class="text-muted mb-0">Actions rapides</h6>
                                <a href="{{ path('app_mode_de_paiement_edit', {'id': mode_de_paiement.id}) }}" class="btn btn-warning btn-lg py-3" style="margin: 0 3rem;">
                                    <i class="fas fa-edit me-2"></i>Modifier
                                </a>
                                <a href="{{ path('app_mode_de_paiement_index') }}" class="btn btn-secondary btn-lg py-3" style="margin: 0 3rem;">
                                    <i class="fas fa-arrow-left me-2"></i>Retour à la liste
                                </a>
                                <div class="mt-3">
                                    {{ include('mode_de_paiement/_delete_form.html.twig') }}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
