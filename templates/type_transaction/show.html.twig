{% extends 'base.html.twig' %}

{% block title %}TypeTransaction{% endblock %}

{% block body %}
    <h1>TypeTransaction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_type</th>
                <td>{{ type_transaction.idType }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_transaction.libelle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_transaction.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_transaction_index') }}">back to list</a>

    <a href="{{ path('app_type_transaction_edit', {'id_type': type_transaction.idType}) }}">edit</a>

    {{ include('type_transaction/_delete_form.html.twig') }}
{% endblock %}
