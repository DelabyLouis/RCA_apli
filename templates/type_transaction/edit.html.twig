{% extends 'base.html.twig' %}{% extends 'base.html.twig' %}



{% block title %}Modifier {{ type_transaction.libelle }}{% endblock %}{% block title %}Modifier {{ type_transaction.libelle }}{% endblock %}



{% block body %}{% block body %}

<div class="container-fluid"><div class="container-fluid">

    <div class="row">    <div class="row">

        <div class="col-12">        <div class="col-12">

            <div class="d-flex justify-content-between align-items-center mb-4">            <div class="d-flex justify-content-between align-items-center mb-4">

                <h1><i class="fas fa-edit me-2"></i>Modifier {{ type_transaction.libelle }}</h1>                <h1><i class="fas fa-edit me-2"></i>Modifier {{ type_transaction.libelle }}</h1>

                <a href="{{ path('app_type_transaction_index') }}" class="btn btn-secondary">                <a href="{{ path('app_type_transaction_index') }}" class="btn btn-secondary">

                    <i class="fas fa-arrow-left me-1"></i>Retour à la liste                    <i class="fas fa-arrow-left me-1"></i>Retour à la liste

                </a>                </a>

            </div>            </div>



            {% if form.vars.errors|length > 0 %}            <div class="card shadow-sm">

                <div class="alert alert-danger alert-dismissible fade show" role="alert">                <div class="card-body">

                    <i class="fas fa-exclamation-triangle me-2"></i><strong>Erreurs de validation :</strong>                    <div class="row">

                    <ul class="mb-0 mt-2">                        <div class="col-lg-6">

                        {% for error in form.vars.errors %}                            {{ form_start(form, {'attr': {'novalidate': false, 'id': 'type_transaction_edit_form'}}) }}

                            <li>{{ error.message }}</li>                            

                        {% endfor %}                            <div class="mb-3">

                    </ul>                                {{ form_label(form.libelle, 'Libellé', {'label_attr': {'class': 'form-label'}}) }}

                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>                                {{ form_widget(form.libelle, {'attr': {'class': 'form-control' ~ (form.libelle.vars.errors|length > 0 ? ' is-invalid' : '')}}) }}

                </div>                                {% if form.libelle.vars.errors|length > 0 %}

            {% endif %}                                    <div class="invalid-feedback">

                                        {% for error in form.libelle.vars.errors %}

            <div class="card shadow-sm">                                            {{ error.message }}

                <div class="card-body">                                        {% endfor %}

                    <div class="row">                                    </div>

                        <div class="col-lg-6">                                {% endif %}

                            {{ form_start(form, {'attr': {'novalidate': false, 'id': 'type_transaction_edit_form'}}) }}                                {{ form_errors(form.libelle) }}

                                                        </div>

                            <div class="mb-3">                            

                                {{ form_label(form.libelle, 'Libellé', {'label_attr': {'class': 'form-label'}}) }}                            <div class="mb-3">

                                {{ form_widget(form.libelle, {'attr': {'class': 'form-control' ~ (form.libelle.vars.errors|length > 0 ? ' is-invalid' : '')}}) }}                                {{ form_label(form.description, 'Description', {'label_attr': {'class': 'form-label'}}) }}

                                {% if form.libelle.vars.errors|length > 0 %}                                {{ form_widget(form.description, {'attr': {'class': 'form-control' ~ (form.description.vars.errors|length > 0 ? ' is-invalid' : ''), 'rows': 3}}) }}

                                    <div class="invalid-feedback">                                {% if form.description.vars.errors|length > 0 %}

                                        {% for error in form.libelle.vars.errors %}                                    <div class="invalid-feedback">

                                            {{ error.message }}                                        {% for error in form.description.vars.errors %}

                                        {% endfor %}                                            {{ error.message }}

                                    </div>                                        {% endfor %}

                                {% endif %}                                    </div>

                                {{ form_errors(form.libelle) }}                                {% endif %}

                            </div>                                {{ form_errors(form.description) }}

                                                        </div>

                            <div class="mb-3">                            

                                {{ form_label(form.description, 'Description', {'label_attr': {'class': 'form-label'}}) }}                            <div class="mb-4">

                                {{ form_widget(form.description, {'attr': {'class': 'form-control' ~ (form.description.vars.errors|length > 0 ? ' is-invalid' : '')}}) }}                                {{ form_label(form.type_montant_autorise, null, {'label_attr': {'class': 'form-label fw-bold mb-3'}}) }}

                                {% if form.description.vars.errors|length > 0 %}                                <div class="alert alert-info mb-3">

                                    <div class="invalid-feedback">                                    <i class="fas fa-info-circle me-2"></i>

                                        {% for error in form.description.vars.errors %}                                    <strong>Important :</strong> Si vous changez cette contrainte, les transactions existantes qui ne respectent plus la nouvelle règle perdront automatiquement leur type et devront être reassignées manuellement.

                                            {{ error.message }}                                </div>

                                        {% endfor %}                                <div class="mt-3">

                                    </div>                                    {% for child in form.type_montant_autorise %}

                                {% endif %}                                        <div class="form-check mb-3 p-3 border rounded bg-light">

                                {{ form_errors(form.description) }}                                            {{ form_widget(child, {'attr': {'class': 'form-check-input me-3'}}) }}

                            </div>                                            {{ form_label(child, null, {'label_attr': {'class': 'form-check-label fw-semibold'}}) }}

                                                                    </div>

                            <div class="mb-3">                                    {% endfor %}

                                {{ form_label(form.type_montant_autorise, 'Type de montant autorisé', {'label_attr': {'class': 'form-label'}}) }}                                </div>

                                {{ form_widget(form.type_montant_autorise, {'attr': {'class': 'form-select' ~ (form.type_montant_autorise.vars.errors|length > 0 ? ' is-invalid' : '')}}) }}                                {% if form.type_montant_autorise.vars.errors|length > 0 %}

                                {% if form.type_montant_autorise.vars.errors|length > 0 %}                                    <div class="invalid-feedback d-block">

                                    <div class="invalid-feedback">                                        {% for error in form.type_montant_autorise.vars.errors %}

                                        {% for error in form.type_montant_autorise.vars.errors %}                                            {{ error.message }}

                                            {{ error.message }}                                        {% endfor %}

                                        {% endfor %}                                    </div>

                                    </div>                                {% endif %}

                                {% endif %}                                {{ form_errors(form.type_montant_autorise) }}

                                {{ form_errors(form.type_montant_autorise) }}                            </div>

                            </div>                            

                                                        {{ form_end(form) }}

                            {{ form_end(form) }}                        </div>

                        </div>                        

                                                <div class="col-lg-6">

                        <div class="col-lg-6">                            <div class="d-flex flex-column gap-3 sticky-top px-5" style="top: 20px;">

                            <div class="d-flex flex-column gap-3 sticky-top px-5" style="top: 20px;">                                <h6 class="text-muted mb-0">Actions</h6>

                                <h6 class="text-muted mb-0">Actions</h6>                                <a href="{{ path('app_type_transaction_index') }}" class="btn btn-secondary btn-lg py-3" style="margin: 0 3rem;">

                                <a href="{{ path('app_type_transaction_show', {'id_type': type_transaction.idType}) }}" class="btn btn-info btn-lg py-3" style="margin: 0 3rem;">                                    <i class="fas fa-times me-2"></i>Annuler

                                    <i class="fas fa-eye me-2"></i>Voir le type                                </a>

                                </a>                                <a href="{{ path('app_type_transaction_show', {'id_type': type_transaction.idType}) }}" class="btn btn-info btn-lg py-3" style="margin: 0 3rem;">

                                <a href="{{ path('app_type_transaction_index') }}" class="btn btn-secondary btn-lg py-3" style="margin: 0 3rem;">                                    <i class="fas fa-eye me-2"></i>Voir le détail

                                    <i class="fas fa-times me-2"></i>Annuler                                </a>

                                </a>                                <button type="submit" class="btn btn-warning btn-lg py-3" style="margin: 0 3rem;" form="type_transaction_edit_form">

                                <button type="submit" class="btn btn-warning btn-lg py-3" style="margin: 0 3rem;" form="type_transaction_edit_form">                                    <i class="fas fa-save me-2"></i>Mettre à jour

                                    <i class="fas fa-save me-2"></i>Mettre à jour                                </button>

                                </button>                            </div>

                            </div>                        </div>

                        </div>                    </div>

                    </div>                </div>

                </div>            </div>

            </div>        </div>

        </div>    </div>

    </div></div>

</div>{% endblock %}
{% endblock %}