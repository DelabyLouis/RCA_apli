{% extends 'base.html.twig' %}

{% block title %}Modifier {{ type_transaction.libelle }}{% endblock %}

{% block body %}
<div class="container-fluid">
    <h1 class="mb-4">
        <i class="fas fa-edit me-2 text-warning"></i>Modifier le type : {{ type_transaction.libelle }}
    </h1>

    <div class="row justify-content-center">
        <div class="col-md-10 col-lg-8">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">Modifier le type de transaction</h5>
                </div>
                <div class="card-body">
                    {{ form_start(form) }}
                    
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                {{ form_label(form.libelle, 'Libellé') }}
                                {{ form_widget(form.libelle, {'attr': {'class': 'form-control'}}) }}
                                {{ form_errors(form.libelle) }}
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                {{ form_label(form.typeMontantAutorise, 'Type de montant autorisé') }}
                                <div class="form-check-container">
                                    {{ form_widget(form.typeMontantAutorise) }}
                                </div>
                                {{ form_errors(form.typeMontantAutorise) }}
                            </div>
                        </div>
                    </div>

                    <div class="d-flex justify-content-between mt-4">
                        <div>
                            <a href="{{ path('app_type_transaction_index') }}" class="btn btn-outline-secondary btn-lg me-2">
                                <i class="fas fa-list me-2"></i>Liste
                            </a>
                            <a href="{{ path('app_type_transaction_show', {'id_type': type_transaction.idType}) }}" class="btn btn-outline-info btn-lg">
                                <i class="fas fa-eye me-2"></i>Affichage
                            </a>
                        </div>
                        <div>
                            <button type="submit" class="btn btn-warning btn-lg">
                                <i class="fas fa-save me-2"></i>Mettre à jour
                            </button>
                        </div>
                    </div>

                    {{ form_rest(form) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.form-check-container .form-check {
    margin-bottom: 0.5rem;
}
.form-check-container .form-check-input {
    margin-top: 0.3rem;
}
.form-check-container .form-check-label {
    margin-left: 0.5rem;
}
</style>
{% endblock %}