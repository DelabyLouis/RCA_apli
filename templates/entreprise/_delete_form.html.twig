<form method="post" action="{{ path('app_entreprise_delete', {'id_entreprise': entreprise.id_entreprise}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entreprise.id_entreprise) }}">
    <button class="btn">Delete</button>
</form>
