{% extends 'base.html.twig' %}

{% block title %}Exercice{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .editable-field {
            cursor: pointer;
            padding: 8px;
            border: 1px solid transparent;
            border-radius: 4px;
            transition: all 0.2s;
            min-height: 20px;
            display: inline-block;
            min-width: 200px;
        }
        
        .editable-field:hover {
            background-color: #f8f9fa;
            border-color: #dee2e6;
        }
        
        .editable-field.editing {
            background-color: #fff;
            border-color: #007bff;
            box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
        }
        
        .editable-input {
            width: 100%;
            border: none;
            outline: none;
            background: transparent;
            font-size: inherit;
            font-family: inherit;
            min-width: 200px;
        }
        
        .saving {
            opacity: 0.6;
            pointer-events: none;
        }
        
        .alert {
            margin: 15px 0;
            padding: 12px;
            border-radius: 4px;
            border: 1px solid transparent;
        }
        
        .alert-success {
            background-color: #d4edda;
            border-color: #c3e6cb;
            color: #155724;
        }
        
        .alert-danger {
            background-color: #f8d7da;
            border-color: #f5c6cb;
            color: #721c24;
        }
        
        .table td {
            vertical-align: middle;
        }
        
        .readonly-field {
            background-color: #f8f9fa;
            color: #6c757d;
            padding: 8px;
            border-radius: 4px;
        }
    </style>
{% endblock %}

{% block body %}
    <h1>Exercice</h1>
    
    <div id="message-container"></div>

    <table class="table" data-id="{{ exercice.idExercice }}">
        <tbody>
            <tr>
                <th width="150">Id_exercice</th>
                <td>
                    <span class="readonly-field">{{ exercice.idExercice }}</span>
                </td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>
                    <span class="editable-field" data-field="libelle">{{ exercice.libelle }}</span>
                </td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>
                    <span class="editable-field" data-field="date_debut">{{ exercice.dateDebut ? exercice.dateDebut|date('Y-m-d') : '' }}</span>
                </td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>
                    <span class="editable-field" data-field="date_fin">{{ exercice.dateFin ? exercice.dateFin|date('Y-m-d') : '' }}</span>
                </td>
            </tr>
        </tbody>
    </table>

    <div class="mt-3">
        <a href="{{ path('app_exercice_index') }}" class="btn btn-secondary">back to list</a>
        <a href="{{ path('app_exercice_edit', {'id_exercice': exercice.idExercice}) }}" class="btn btn-primary">edit</a>
        {{ include('exercice/_delete_form.html.twig') }}
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            let currentEditingField = null;
            let originalValue = '';
            
            function showMessage(message, type) {
                const container = document.getElementById('message-container');
                container.innerHTML = '';
                
                const alert = document.createElement('div');
                alert.className = `alert alert-${type}`;
                alert.textContent = message;
                container.appendChild(alert);
                
                setTimeout(() => {
                    alert.remove();
                }, 4000);
            }
            
            function saveField(field, value, exerciceId) {
                const table = document.querySelector('.table');
                table.classList.add('saving');
                
                fetch(`/exercice/${exerciceId}/update-field`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/x-www-form-urlencoded',
                    },
                    body: `field=${encodeURIComponent(field)}&value=${encodeURIComponent(value)}`
                })
                .then(response => response.json())
                .then(data => {
                    table.classList.remove('saving');
                    
                    if (data.success) {
                        currentEditingField.textContent = value || '';
                        showMessage(data.message, 'success');
                    } else {
                        currentEditingField.textContent = originalValue;
                        showMessage(data.message, 'danger');
                    }
                })
                .catch(error => {
                    table.classList.remove('saving');
                    currentEditingField.textContent = originalValue;
                    showMessage('Erreur de communication avec le serveur', 'danger');
                    console.error('Error:', error);
                });
            }
            
            function startEditing(field) {
                if (currentEditingField && currentEditingField !== field) {
                    stopEditing(false);
                }
                
                currentEditingField = field;
                originalValue = field.textContent;
                
                field.classList.add('editing');
                
                const input = document.createElement('input');
                input.className = 'editable-input';
                
                if (field.dataset.field.includes('date')) {
                    input.type = 'date';
                    input.value = originalValue;
                } else {
                    input.type = 'text';
                    input.value = originalValue;
                }
                
                field.textContent = '';
                field.appendChild(input);
                
                input.focus();
                if (input.type === 'text') {
                    input.select();
                }
                
                input.addEventListener('blur', function() {
                    stopEditing(true);
                });
                
                input.addEventListener('keydown', function(e) {
                    if (e.key === 'Enter') {
                        e.preventDefault();
                        stopEditing(true);
                    } else if (e.key === 'Escape') {
                        e.preventDefault();
                        stopEditing(false);
                    }
                });
            }
            
            function stopEditing(save) {
                if (!currentEditingField || !currentEditingField.classList.contains('editing')) return;
                
                const input = currentEditingField.querySelector('input');
                const newValue = input ? input.value.trim() : '';
                
                currentEditingField.classList.remove('editing');
                
                if (save && newValue !== originalValue) {
                    const fieldName = currentEditingField.dataset.field;
                    const exerciceId = document.querySelector('.table').dataset.id;
                    
                    if (fieldName === 'libelle' && !newValue) {
                        showMessage('Le libellé ne peut pas être vide', 'danger');
                        currentEditingField.textContent = originalValue;
                    } else {
                        saveField(fieldName, newValue, exerciceId);
                    }
                } else {
                    currentEditingField.textContent = originalValue;
                }
                
                currentEditingField = null;
            }
            
            document.querySelectorAll('.editable-field').forEach(field => {
                field.addEventListener('click', function() {
                    if (!this.classList.contains('editing')) {
                        startEditing(this);
                    }
                });
            });
            
            document.addEventListener('click', function(e) {
                if (currentEditingField && 
                    !currentEditingField.contains(e.target) && 
                    !e.target.classList.contains('editable-field')) {
                    stopEditing(false);
                }
            });
        });
    </script>
{% endblock %}
