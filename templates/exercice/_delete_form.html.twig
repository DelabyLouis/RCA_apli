<form method="post" action="{{ path('app_exercice_delete', {'id_exercice': exercice.id_exercice}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exercice.id_exercice) }}">
    <button class="btn">Delete</button>
</form>
