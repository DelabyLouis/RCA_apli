{% extends 'base.html.twig' %}{% extends 'base.html.twig' %}{% extends 'base.html.twig' %}{% extends 'base.html.twig' %}{% extends 'base.html.twig' %}



{% block title %}Modifier la transaction{% endblock %}



{% block body %}{% block title %}Modifier la transaction{% endblock %}

<div class="container-fluid">

    <div class="row">

        <div class="col-lg-6">

            <div class="card">{% block body %}{% block title %}Modifier la transaction{% endblock %}

                <div class="card-header">

                    <h4 class="mb-0"><div class="container-fluid">

                        <i class="fas fa-edit me-2"></i>Modifier la transaction

                    </h4>    <div class="row">

                </div>

                <div class="card-body">        <div class="col-lg-6">

                    {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': true}}) }}

                                <div class="card">{% block body %}{% block title %}Modifier la transaction{% endblock %}{% block title %}Modifier la transaction{% endblock %}

                    <div class="row mb-3">

                        <div class="col-md-6">                <div class="card-header">

                            {{ form_row(form.libelle) }}

                        </div>                    <h4 class="mb-0"><div class="container-fluid">

                        <div class="col-md-6">

                            {{ form_row(form.montant) }}                        <i class="fas fa-edit me-2"></i>Modifier la transaction

                        </div>

                    </div>                    </h4>    <div class="row">



                    <div class="row mb-3">                </div>

                        <div class="col-md-6">

                            {{ form_row(form.personne) }}                <div class="card-body">        <div class="col-12">

                        </div>

                        <div class="col-md-6">                    {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': true}}) }}

                            {{ form_row(form.entreprise) }}

                        </div>                                <div class="d-flex justify-content-between align-items-center mb-4">{% block body %}{% block body %}

                    </div>

                    <div class="row mb-3">

                    <div class="row mb-3">

                        <div class="col-md-6">                        <div class="col-md-6">                <h1><i class="fas fa-edit me-2"></i>Modifier la transaction #{{ transaction.idTransaction }}</h1>

                            {{ form_row(form.typeTransaction) }}

                        </div>                            {{ form_row(form.libelle, {'attr': {'class': 'form-control'}}) }}

                        <div class="col-md-6">

                            {{ form_row(form.modeDePaiement) }}                        </div>                <a href="{{ path('app_transaction_index') }}" class="btn btn-secondary"><div class="container-fluid mt-4"><div class="container-fluid">

                        </div>

                    </div>                        <div class="col-md-6">



                    <div class="row mb-3">                            {{ form_row(form.montant, {'attr': {'class': 'form-control', 'step': '0.01', 'id': 'montant'}}) }}                    <i class="fas fa-arrow-left me-1"></i>Retour à la liste

                        <div class="col-md-6">

                            {{ form_row(form.exercice) }}                        </div>

                        </div>

                        <div class="col-md-6">                    </div>                </a>    <div class="row">    <div class="row">

                            {{ form_row(form.dateTransaction) }}

                        </div>

                    </div>

                    <div class="row mb-3">            </div>

                    <div class="row mb-3">

                        <div class="col-md-6">                        <div class="col-md-6">

                            {{ form_row(form.numeroCompte) }}

                        </div>                            {{ form_row(form.personne, {'attr': {'class': 'form-select'}}) }}        <div class="col-lg-6">        <div class="col-12">

                        <div class="col-md-6"></div>

                    </div>                        </div>



                    <div class="row mb-4">                        <div class="col-md-6">            <div class="card shadow-sm">

                        <div class="col-12">

                            {{ form_row(form.description) }}                            {{ form_row(form.entreprise, {'attr': {'class': 'form-select'}}) }}

                        </div>

                    </div>                        </div>                <div class="card-body">            <h1><i class="fas fa-edit me-2"></i>Modifier la transaction #{{ transaction.idTransaction }}</h1>            <div class="d-flex justify-content-between align-items-center mb-4">



                    {{ form_rest(form) }}                    </div>

                    {{ form_end(form) }}

                </div>                    <div class="row">

            </div>

        </div>                    <div class="row mb-3">



        <div class="col-lg-6">                        <div class="col-md-6">                        <div class="col-lg-6">                <h1><i class="fas fa-edit me-2"></i>Modifier la transaction</h1>

            <div class="sticky-top" style="top: 1rem;">

                <div class="card">                            {{ form_row(form.typeTransaction, {'attr': {'class': 'form-select'}}) }}

                    <div class="card-header">

                        <h5 class="mb-0">Actions</h5>                        </div>                            {{ form_start(form, {'attr': {'novalidate': false, 'id': 'transaction_edit_form'}}) }}

                    </div>

                    <div class="card-body">                        <div class="col-md-6">

                        <div class="d-grid gap-2">

                            <button type="submit" class="btn btn-primary btn-lg" form="{{ form.vars.id }}">                            {{ form_row(form.modeDePaiement, {'attr': {'class': 'form-select'}}) }}                                        {% for flash_error in app.flashes('error') %}                <a href="{{ path('app_transaction_index') }}" class="btn btn-secondary">

                                <i class="fas fa-save me-2"></i>Enregistrer les modifications

                            </button>                        </div>

                            <a href="{{ path('app_transaction_show', {'id': transaction.id}) }}" class="btn btn-info">

                                <i class="fas fa-eye me-2"></i>Voir la transaction                    </div>                            <div class="mb-3">

                            </a>

                            <a href="{{ path('app_transaction_index') }}" class="btn btn-secondary">

                                <i class="fas fa-arrow-left me-2"></i>Retour à la liste

                            </a>                    <div class="row mb-3">                                {{ form_label(form.libelle, 'Libellé', {'label_attr': {'class': 'form-label'}}) }}                <div class="alert alert-danger">{{ flash_error }}</div>                    <i class="fas fa-arrow-left me-1"></i>Retour à la liste

                        </div>

                                                <div class="col-md-6">

                        <hr class="my-4">

                                                    {{ form_row(form.exercice, {'attr': {'class': 'form-select'}}) }}                                {{ form_widget(form.libelle, {'attr': {'class': 'form-control' ~ (form.libelle.vars.errors|length > 0 ? ' is-invalid' : '')}}) }}

                        <div class="card">

                            <div class="card-body">                        </div>

                                <h6 class="card-title">

                                    <i class="fas fa-info-circle me-2"></i>Informations                        <div class="col-md-6">                                {{ form_errors(form.libelle) }}            {% endfor %}                </a>

                                </h6>

                                <p class="card-text small mb-2">                            {{ form_row(form.dateTransaction, {'attr': {'class': 'form-control'}}) }}

                                    <strong>Transaction #{{ transaction.id }}</strong>

                                </p>                        </div>                            </div>

                                <p class="card-text small mb-2">

                                    Créée le : {{ transaction.dateTransaction ? transaction.dateTransaction|date('d/m/Y') : 'Non définie' }}                    </div>

                                </p>

                                <p class="card-text small mb-0">                                        </div>

                                    Montant actuel : 

                                    <span class="badge {{ transaction.montant >= 0 ? 'bg-success' : 'bg-danger' }}">                    <div class="row mb-3">

                                        {{ transaction.montant|number_format(2, ',', ' ') }} €

                                    </span>                        <div class="col-md-6">                            <div class="mb-3">

                                </p>

                            </div>                            {{ form_row(form.numeroCompte, {'attr': {'class': 'form-control'}}) }}

                        </div>

                        </div>                                {{ form_label(form.date_transaction, 'Date', {'label_attr': {'class': 'form-label'}}) }}            <div class="card">

                        <div class="alert alert-info mt-3">

                            <h6 class="alert-heading">                        <div class="col-md-6"></div>

                                <i class="fas fa-info-circle me-2"></i>Instructions

                            </h6>                    </div>                                {{ form_widget(form.date_transaction, {'attr': {'class': 'form-control' ~ (form.date_transaction.vars.errors|length > 0 ? ' is-invalid' : '')}}) }}

                            <ul class="mb-0 small">

                                <li>Montant <strong>positif</strong> = crédit (entrée d'argent)</li>

                                <li>Montant <strong>négatif</strong> = débit (sortie d'argent)</li>

                                <li>Modifiez directement le signe du montant</li>                    <div class="row mb-4">                                {{ form_errors(form.date_transaction) }}                <div class="card-body">            <div class="card shadow-sm">

                            </ul>

                        </div>                        <div class="col-12">

                    </div>

                </div>                            {{ form_row(form.description, {'attr': {'class': 'form-control', 'rows': 3}}) }}                            </div>

            </div>

        </div>                        </div>

    </div>

</div>                    </div>                    {{ form_start(form) }}                <div class="card-body">

{% endblock %}


                    <div class="row mb-4">                            <div class="mb-3">

                        <div class="col-12">

                            <label class="form-label">Type de transaction</label>                                <label class="form-label">Type de transaction</label>                    {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': true}}) }}

                            <div class="btn-group w-100" role="group">

                                <button type="button" class="btn btn-outline-success" id="creditBtn" onclick="setCredit()">                                <div class="row g-2">

                                    <i class="fas fa-plus-circle me-2"></i>CRÉDIT

                                </button>                                    <div class="col-6">                    <div class="mb-3">                    

                                <button type="button" class="btn btn-outline-danger" id="debitBtn" onclick="setDebit()">

                                    <i class="fas fa-minus-circle me-2"></i>DÉBIT                                        <div class="d-grid">

                                </button>

                            </div>                                            <button type="button" id="credit-btn" class="btn {{ transaction.montant >= 0 ? 'btn-success active' : 'btn-outline-success' }} btn-lg" onclick="setCredit()">                        {{ form_label(form.libelle, 'Libellé', {'label_attr': {'class': 'form-label'}}) }}                    <div class="row">

                        </div>

                    </div>                                                <i class="fas fa-plus me-2"></i>CRÉDIT



                    {{ form_rest(form) }}                                                <br><small>Entrée d'argent</small>                        {{ form_widget(form.libelle, {'attr': {'class': 'form-control'}}) }}                        <div class="col-md-6">

                    {{ form_end(form) }}

                </div>                                            </button>

            </div>

        </div>                                        </div>                        {{ form_errors(form.libelle) }}                            <div class="mb-3">



        <div class="col-lg-6">                                    </div>

            <div class="sticky-top" style="top: 1rem;">

                <div class="card">                                    <div class="col-6">                    </div>                                {{ form_label(form.libelle, 'Libellé', {'label_attr': {'class': 'form-label'}}) }}

                    <div class="card-header">

                        <h5 class="mb-0">Actions</h5>                                        <div class="d-grid">

                    </div>

                    <div class="card-body">                                            <button type="button" id="debit-btn" class="btn {{ transaction.montant < 0 ? 'btn-danger active' : 'btn-outline-danger' }} btn-lg" onclick="setDebit()">                                {{ form_widget(form.libelle, {'attr': {'class': 'form-control'}}) }}

                        <div class="d-grid gap-2">

                            <button type="submit" class="btn btn-primary btn-lg" form="{{ form.vars.id }}">                                                <i class="fas fa-minus me-2"></i>DÉBIT

                                <i class="fas fa-save me-2"></i>Enregistrer les modifications

                            </button>                                                <br><small>Sortie d'argent</small>                    <div class="mb-3">                                {{ form_errors(form.libelle) }}

                            <a href="{{ path('app_transaction_show', {'id': transaction.id}) }}" class="btn btn-info">

                                <i class="fas fa-eye me-2"></i>Voir la transaction                                            </button>

                            </a>

                            <a href="{{ path('app_transaction_index') }}" class="btn btn-secondary">                                        </div>                        {{ form_label(form.date_transaction, 'Date', {'label_attr': {'class': 'form-label'}}) }}                            </div>

                                <i class="fas fa-arrow-left me-2"></i>Retour à la liste

                            </a>                                    </div>

                        </div>

                                                        </div>                        {{ form_widget(form.date_transaction, {'attr': {'class': 'form-control'}}) }}                        </div>

                        <hr class="my-4">

                                                        <input type="hidden" id="transaction-type" name="transaction_type" value="{{ transaction.montant >= 0 ? 'credit' : 'debit' }}">

                        <div class="card">

                            <div class="card-body">                                <div class="form-text mt-2 text-center">                        {{ form_errors(form.date_transaction) }}                        <div class="col-md-6">

                                <h6 class="card-title">

                                    <i class="fas fa-info-circle me-2"></i>Informations                                    <small id="type-help" class="{{ transaction.montant >= 0 ? 'text-success' : 'text-danger' }}">

                                </h6>

                                <p class="card-text small mb-2">                                        <i class="fas fa-{{ transaction.montant >= 0 ? 'plus' : 'minus' }} me-1"></i>Mode {{ transaction.montant >= 0 ? 'CRÉDIT' : 'DÉBIT' }} - Montant actuel: {{ transaction.montant }}€                    </div>                            <div class="mb-3">

                                    <strong>Transaction #{{ transaction.id }}</strong>

                                </p>                                    </small>

                                <p class="card-text small mb-2">

                                    Créée le : {{ transaction.dateTransaction ? transaction.dateTransaction|date('d/m/Y') : 'Non définie' }}                                </div>                                {{ form_label(form.exercice, 'Exercice', {'label_attr': {'class': 'form-label'}}) }}

                                </p>

                                <p class="card-text small mb-0">                            </div>

                                    Montant actuel : 

                                    <span class="badge {{ transaction.montant >= 0 ? 'bg-success' : 'bg-danger' }}">                                                <div class="mb-3">                                {{ form_widget(form.exercice, {'attr': {'class': 'form-select'}}) }}

                                        {{ transaction.montant|number_format(2, ',', ' ') }} €

                                    </span>                            <div class="mb-3">

                                </p>

                            </div>                                {{ form_label(form.montant, 'Montant', {'label_attr': {'class': 'form-label'}}) }}                        <label class="form-label">Type de transaction</label>                                {{ form_errors(form.exercice) }}

                        </div>

                    </div>                                <div class="input-group">

                </div>

            </div>                                    {{ form_widget(form.montant, {                        <div class="btn-group w-100 mb-2" role="group" aria-label="Type de transaction">                            </div>

        </div>

    </div>                                        'attr': {

</div>

                                            'class': 'form-control' ~ (form.montant.vars.errors|length > 0 ? ' is-invalid' : ''),                            <input type="radio" class="btn-check" name="transaction_nature" id="credit_edit" value="credit"                         </div>

<script>

let transactionType = null;                                            'step': '0.01',

const currentMontant = {{ transaction.montant }};

                                            'id': 'montant',                                   {{ transaction.montant >= 0 ? 'checked' : '' }}>                    </div>

document.addEventListener('DOMContentLoaded', function() {

    // Set initial state based on current montant                                            'onblur': 'applySign()'

    if (currentMontant >= 0) {

        setCredit();                                        }                            <label class="btn {{ transaction.montant >= 0 ? 'btn-success' : 'btn-outline-success' }}" for="credit_edit">

    } else {

        setDebit();                                    }) }}

    }

});                                    <span class="input-group-text">€</span>                                <i class="fas fa-arrow-down me-2"></i>CRÉDIT (Entrée d'argent)                    <div class="row">



function setCredit() {                                </div>

    transactionType = 'credit';

                                    {{ form_errors(form.montant) }}                            </label>                        <div class="col-md-6">

    document.getElementById('creditBtn').classList.remove('btn-outline-success');

    document.getElementById('creditBtn').classList.add('btn-success');                                <div class="form-text">

    document.getElementById('debitBtn').classList.remove('btn-danger');

    document.getElementById('debitBtn').classList.add('btn-outline-danger');                                    <small id="montant-help" class="text-muted">                                                        <div class="mb-3">

    

    updateMontant();                                        Modifiez le montant. Le signe sera ajusté selon le type sélectionné.

}

                                    </small>                            <input type="radio" class="btn-check" name="transaction_nature" id="debit_edit" value="debit"                                {{ form_label(form.type_transaction, 'Type de transaction', {'label_attr': {'class': 'form-label'}}) }}

function setDebit() {

    transactionType = 'debit';                                </div>

    

    document.getElementById('debitBtn').classList.remove('btn-outline-danger');                            </div>                                   {{ transaction.montant < 0 ? 'checked' : '' }}>                                {{ form_widget(form.type_transaction, {'attr': {'class': 'form-select'}}) }}

    document.getElementById('debitBtn').classList.add('btn-danger');

    document.getElementById('creditBtn').classList.remove('btn-success');                            

    document.getElementById('creditBtn').classList.add('btn-outline-success');

                                <div class="mb-3">                            <label class="btn {{ transaction.montant < 0 ? 'btn-danger' : 'btn-outline-danger' }}" for="debit_edit">                                {{ form_errors(form.type_transaction) }}

    updateMontant();

}                                {{ form_label(form.exercice, 'Exercice', {'label_attr': {'class': 'form-label'}}) }}



function updateMontant() {                                {{ form_widget(form.exercice, {'attr': {'class': 'form-select' ~ (form.exercice.vars.errors|length > 0 ? ' is-invalid' : '')}}) }}                                <i class="fas fa-arrow-up me-2"></i>DÉBIT (Sortie d'argent)                            </div>

    const montantField = document.getElementById('montant');

    if (!montantField || !transactionType) return;                                {{ form_errors(form.exercice) }}

    

    let value = parseFloat(montantField.value) || 0;                            </div>                            </label>                        </div>

    let absValue = Math.abs(value);

                                

    if (transactionType === 'credit') {

        montantField.value = absValue;                            <div class="mb-3">                        </div>                        <div class="col-md-6">

    } else if (transactionType === 'debit') {

        montantField.value = -absValue;                                {{ form_label(form.type_transaction, 'Catégorie', {'label_attr': {'class': 'form-label'}}) }}

    }

}                                {{ form_widget(form.type_transaction, {'attr': {'class': 'form-select' ~ (form.type_transaction.vars.errors|length > 0 ? ' is-invalid' : '')}}) }}                    </div>                            <div class="mb-3">



document.addEventListener('DOMContentLoaded', function() {                                {{ form_errors(form.type_transaction) }}

    const montantField = document.getElementById('montant');

    if (montantField) {                            </div>                                <label class="form-label fw-bold">Montant et Type</label>

        montantField.addEventListener('input', updateMontant);

    }

});

</script>                            <div class="mb-3">                    <div class="mb-3">                                

{% endblock %}
                                {{ form_label(form.personne, 'Personne', {'label_attr': {'class': 'form-label'}}) }}

                                {{ form_widget(form.personne, {'attr': {'class': 'form-select' ~ (form.personne.vars.errors|length > 0 ? ' is-invalid' : '')}}) }}                        <label class="form-label">Montant (€)</label>                                <!-- Boutons radio pour Débit/Crédit -->

                                {{ form_errors(form.personne) }}

                            </div>                        <div class="input-group">                                <div class="btn-group w-100 mb-2" role="group">



                            <div class="mb-3">                            <input type="number"                                     <input type="radio" class="btn-check" name="transaction_type_edit" id="credit-edit" value="credit" 

                                {{ form_label(form.entreprise, 'Entreprise', {'label_attr': {'class': 'form-label'}}) }}

                                {{ form_widget(form.entreprise, {'attr': {'class': 'form-select' ~ (form.entreprise.vars.errors|length > 0 ? ' is-invalid' : '')}}) }}                                   id="montant_display"                                            {{ (transaction.montant >= 0) ? 'checked' : '' }}>

                                {{ form_errors(form.entreprise) }}

                            </div>                                   class="form-control"                                     <label class="btn {{ (transaction.montant >= 0) ? 'btn-success' : 'btn-outline-success' }}" for="credit-edit">

                            

                            {{ form_end(form) }}                                   step="0.01"                                         <i class="fas fa-plus-circle me-2"></i>CRÉDIT (Entrée)

                        </div>

                                                           min="0.01"                                    </label>

                        <div class="col-lg-6">

                            <div class="d-flex flex-column gap-3 sticky-top px-5" style="top: 20px;">                                   value="{{ transaction.montant|abs }}"                                    

                                <h6 class="text-muted mb-0">Actions</h6>

                                <button type="submit" class="btn btn-warning btn-lg py-3" style="margin: 0 3rem;" form="transaction_edit_form">                                   required>                                    <input type="radio" class="btn-check" name="transaction_type_edit" id="debit-edit" value="debit"

                                    <i class="fas fa-save me-2"></i>Sauvegarder

                                </button>                            <span class="input-group-text">€</span>                                           {{ (transaction.montant < 0) ? 'checked' : '' }}>

                                <a href="{{ path('app_transaction_index') }}" class="btn btn-secondary btn-lg py-3" style="margin: 0 3rem;">

                                    <i class="fas fa-times me-2"></i>Annuler                        </div>                                    <label class="btn {{ (transaction.montant < 0) ? 'btn-danger' : 'btn-outline-danger' }}" for="debit-edit">

                                </a>

                                <a href="{{ path('app_transaction_show', {'id_transaction': transaction.idTransaction}) }}" class="btn btn-info btn-lg py-3" style="margin: 0 3rem;">                        <!-- Champ caché pour le vrai montant -->                                        <i class="fas fa-minus-circle me-2"></i>DÉBIT (Sortie)

                                    <i class="fas fa-eye me-2"></i>Voir les détails

                                </a>                        {{ form_widget(form.montant, {'attr': {'style': 'display:none', 'id': 'montant_real'}}) }}                                    </label>

                                

                                <div class="card mt-4">                        {{ form_errors(form.montant) }}                                </div>

                                    <div class="card-body">

                                        <h6 class="card-title"><i class="fas fa-info-circle me-2"></i>Transaction actuelle</h6>                        <div class="form-text">                                

                                        <ul class="small mb-0">

                                            <li><strong>Montant :</strong> {{ transaction.montant }}€</li>                            <span id="montant_help_edit">Saisissez un montant positif. Le signe sera automatiquement ajusté selon le type choisi.</span>                                <!-- Champ pour le montant (toujours positif) -->

                                            <li><strong>Type :</strong> {{ transaction.montant >= 0 ? 'CRÉDIT' : 'DÉBIT' }}</li>

                                            <li><strong>Exercice :</strong> {{ transaction.exercice.libelle }}</li>                        </div>                                <div class="input-group">

                                            <li><strong>Créée le :</strong> {{ transaction.dateTransaction|date('d/m/Y') }}</li>

                                        </ul>                    </div>                                    <input type="number" 

                                    </div>

                                </div>                                           id="montant-display-edit" 

                            </div>

                        </div>                    <div class="mb-3">                                           class="form-control" 

                    </div>

                </div>                        {{ form_label(form.exercice, 'Exercice', {'label_attr': {'class': 'form-label'}}) }}                                           placeholder="0.00" 

            </div>

        </div>                        {{ form_widget(form.exercice, {'attr': {'class': 'form-select'}}) }}                                           step="0.01" 

    </div>

</div>                        {{ form_errors(form.exercice) }}                                           min="0"



<script>                    </div>                                           value="{{ transaction.montant ? (transaction.montant|abs) : '' }}"

var currentType = '{{ transaction.montant >= 0 ? "credit" : "debit" }}'; // État initial basé sur la transaction

                                           required>

function setCredit() {

    currentType = 'credit';                    <div class="mb-3">                                    <span class="input-group-text">€</span>

    

    // Mise à jour visuelle                        {{ form_label(form.type_transaction, 'Catégorie', {'label_attr': {'class': 'form-label'}}) }}                                </div>

    document.getElementById('credit-btn').className = 'btn btn-success btn-lg active';

    document.getElementById('debit-btn').className = 'btn btn-outline-danger btn-lg';                        {{ form_widget(form.type_transaction, {'attr': {'class': 'form-select'}}) }}                                

    

    // Mise à jour du champ caché                        {{ form_errors(form.type_transaction) }}                                <!-- Champ caché pour le montant réel (avec signe) -->

    document.getElementById('transaction-type').value = 'credit';

                        </div>                                <div style="display: none;">

    // Mise à jour de l'aide

    document.getElementById('type-help').innerHTML = '<i class="fas fa-plus me-1"></i>Mode CRÉDIT - montant sera positif';                                    {{ form_widget(form.montant, {'attr': {'id': 'montant-hidden-edit'}}) }}

    document.getElementById('type-help').className = 'text-success';

                        <div class="mb-3">                                </div>

    // Appliquer le signe au montant

    applySign();                        <label class="form-label">Tiers (Personne ou Entreprise)</label>                                

    

    console.log('Mode CRÉDIT activé');                        <select id="tiers_selector" class="form-select">                                {{ form_errors(form.montant) }}

}

                            <option value="">-- Sélectionner un tiers --</option>                            </div>

function setDebit() {

    currentType = 'debit';                            <optgroup label="Personnes">                        </div>

    

    // Mise à jour visuelle                                {% for personne in form.personne.vars.choices %}                    </div>

    document.getElementById('debit-btn').className = 'btn btn-danger btn-lg active';

    document.getElementById('credit-btn').className = 'btn btn-outline-success btn-lg';                                    <option value="personne-{{ personne.value }}" 

    

    // Mise à jour du champ caché                                            {{ form.personne.vars.value == personne.value ? 'selected' : '' }}>                    <div class="row">

    document.getElementById('transaction-type').value = 'debit';

                                            👤 {{ personne.label }}                        <div class="col-md-6">

    // Mise à jour de l'aide

    document.getElementById('type-help').innerHTML = '<i class="fas fa-minus me-1"></i>Mode DÉBIT - montant sera négatif';                                    </option>                            <div class="mb-3">

    document.getElementById('type-help').className = 'text-danger';

                                    {% endfor %}                                {{ form_label(form.date_transaction, 'Date', {'label_attr': {'class': 'form-label'}}) }}

    // Appliquer le signe au montant

    applySign();                            </optgroup>                                {{ form_widget(form.date_transaction, {'attr': {'class': 'form-control'}}) }}

    

    console.log('Mode DÉBIT activé');                            <optgroup label="Entreprises">                                {{ form_errors(form.date_transaction) }}

}

                                {% for entreprise in form.entreprise.vars.choices %}                            </div>

function applySign() {

    var montantField = document.getElementById('montant');                                    <option value="entreprise-{{ entreprise.value }}"                         </div>

    if (!montantField.value || montantField.value === '0') return;

                                                {{ form.entreprise.vars.value == entreprise.value ? 'selected' : '' }}>                    </div>

    var value = Math.abs(parseFloat(montantField.value));

    if (isNaN(value)) return;                                        🏢 {{ entreprise.label }}

    

    if (currentType === 'debit') {                                    </option>                    <div class="row">

        montantField.value = -value;

        console.log('Montant converti en débit:', -value);                                {% endfor %}                        <div class="col-md-12">

    } else {

        montantField.value = value;                            </optgroup>                            <div class="mb-3">

        console.log('Montant converti en crédit:', value);

    }                        </select>                                <label class="form-label fw-bold">Tiers</label>

}

</script>                                                        <select id="tiers-selector" class="form-select">



{% endblock %}                        <!-- Champs cachés -->                                    <option value="">-- Sélectionner un tiers --</option>

                        {{ form_widget(form.personne, {'attr': {'style': 'display:none'}}) }}                                    <optgroup label="Personnes">

                        {{ form_widget(form.entreprise, {'attr': {'style': 'display:none'}}) }}                                        {% for personne in form.personne.vars.choices %}

                        {{ form_errors(form.personne) }}                                            <option value="personne-{{ personne.value }}" 

                        {{ form_errors(form.entreprise) }}                                                    {% if form.personne.vars.value == personne.value %}selected{% endif %}>

                    </div>                                                👤 {{ personne.label }}

                                            </option>

                    {{ form_end(form) }}                                        {% endfor %}

                </div>                                    </optgroup>

            </div>                                    <optgroup label="Entreprises">

        </div>                                        {% for entreprise in form.entreprise.vars.choices %}

                                                    <option value="entreprise-{{ entreprise.value }}" 

        <div class="col-lg-6">                                                    {% if form.entreprise.vars.value == entreprise.value %}selected{% endif %}>

            <div class="position-sticky" style="top: 20px;">                                                🏢 {{ entreprise.label }}

                <div class="d-grid gap-2">                                            </option>

                    <button type="submit" form="{{ form.vars.id }}" class="btn btn-warning btn-lg">                                        {% endfor %}

                        <i class="fas fa-save me-2"></i>Sauvegarder les modifications                                    </optgroup>

                    </button>                                </select>

                    <a href="{{ path('app_transaction_index') }}" class="btn btn-outline-secondary">                                

                        <i class="fas fa-arrow-left me-2"></i>Retour à la liste                                <!-- Champs cachés pour maintenir la compatibilité avec le formulaire -->

                    </a>                                <div style="display: none;">

                    <a href="{{ path('app_transaction_show', {'id_transaction': transaction.idTransaction}) }}" class="btn btn-outline-info">                                    {{ form_widget(form.personne) }}

                        <i class="fas fa-eye me-2"></i>Voir les détails                                    {{ form_widget(form.entreprise) }}

                    </a>                                </div>

                </div>                                

                                {{ form_errors(form.personne) }}

                <div class="card mt-3">                                {{ form_errors(form.entreprise) }}

                    <div class="card-body">                            </div>

                        <h6 class="card-title">Aide</h6>                        </div>

                        <ul class="small mb-0">                    </div>

                            <li><strong>CRÉDIT :</strong> Argent qui arrive (vente, remboursement, etc.)</li>

                            <li><strong>DÉBIT :</strong> Argent qui sort (achat, charge, etc.)</li>                    <div class="d-flex justify-content-end gap-2">

                            <li>Le montant sera automatiquement signé selon votre choix</li>                        <a href="{{ path('app_transaction_index') }}" class="btn btn-secondary">

                        </ul>                            <i class="fas fa-times me-1"></i>Annuler

                    </div>                        </a>

                </div>                        <button type="submit" class="btn btn-warning">

                            <i class="fas fa-save me-1"></i>Sauvegarder

                <div class="card mt-3">                        </button>

                    <div class="card-header">Informations actuelles</div>                    </div>

                    <div class="card-body">

                        <small class="text-muted">                    {{ form_end(form) }}

                            <strong>Montant actuel :</strong> {{ transaction.montant }} €<br>                </div>

                            <strong>Type :</strong> {{ transaction.montant >= 0 ? 'CRÉDIT (Entrée)' : 'DÉBIT (Sortie)' }}<br>            </div>

                            <strong>Exercice :</strong> {{ transaction.exercice.libelle }}        </div>

                        </small>    </div>

                    </div></div>

                </div>

            </div><script>

        </div>// Gestion du sélecteur de tiers unifié

    </div>document.addEventListener('DOMContentLoaded', function() {

</div>    const tiersSelector = document.getElementById('tiers-selector');

    const personneField = document.querySelector('select[name*="[personne]"]');

<script>    const entrepriseField = document.querySelector('select[name*="[entreprise]"]');

document.addEventListener('DOMContentLoaded', function() {    

    const creditRadio = document.getElementById('credit_edit');    if (tiersSelector && personneField && entrepriseField) {

    const debitRadio = document.getElementById('debit_edit');        // Fonction pour mettre à jour les champs cachés

    const montantDisplay = document.getElementById('montant_display');        function updateHiddenFields() {

    const montantReal = document.getElementById('montant_real');            const selectedValue = tiersSelector.value;

    const montantHelp = document.getElementById('montant_help_edit');            

    const form = document.querySelector('form');            // Réinitialiser les deux champs

    const tiersSelector = document.getElementById('tiers_selector');            personneField.value = '';

    const personneField = form.querySelector('select[name*="[personne]"]');            entrepriseField.value = '';

    const entrepriseField = form.querySelector('select[name*="[entreprise]"]');            

            if (selectedValue.startsWith('personne-')) {

    // Gestion du tiers                personneField.value = selectedValue.replace('personne-', '');

    function updateTiersFields() {            } else if (selectedValue.startsWith('entreprise-')) {

        const selectedValue = tiersSelector.value;                entrepriseField.value = selectedValue.replace('entreprise-', '');

        personneField.value = '';            }

        entrepriseField.value = '';        }

                

        if (selectedValue.startsWith('personne-')) {        // Événement de changement sur le sélecteur unifié

            personneField.value = selectedValue.replace('personne-', '');        tiersSelector.addEventListener('change', updateHiddenFields);

        } else if (selectedValue.startsWith('entreprise-')) {        

            entrepriseField.value = selectedValue.replace('entreprise-', '');        // Initialisation : définir la valeur du sélecteur unifié basée sur les champs existants

        }        function initializeSelector() {

    }            if (personneField.value) {

                tiersSelector.value = 'personne-' + personneField.value;

    // Initialiser le sélecteur de tiers            } else if (entrepriseField.value) {

    if (personneField.value) {                tiersSelector.value = 'entreprise-' + entrepriseField.value;

        tiersSelector.value = 'personne-' + personneField.value;            }

    } else if (entrepriseField.value) {        }

        tiersSelector.value = 'entreprise-' + entrepriseField.value;        

    }        initializeSelector();

    }

    tiersSelector.addEventListener('change', updateTiersFields);    

    // Gestion du montant avec débit/crédit (edit)

    // Gestion du montant    const montantDisplayEdit = document.getElementById('montant-display-edit');

    function updateMontantDisplay() {    const montantHiddenEdit = document.getElementById('montant-hidden-edit');

        const isCredit = creditRadio.checked;    const debitRadioEdit = document.getElementById('debit-edit');

            const creditRadioEdit = document.getElementById('credit-edit');

        if (isCredit) {    

            montantHelp.innerHTML = '<i class="fas fa-arrow-down text-success me-1"></i>CRÉDIT : Le montant sera enregistré comme <strong>positif</strong> (entrée d\'argent)';    if (montantDisplayEdit && montantHiddenEdit && debitRadioEdit && creditRadioEdit) {

            creditRadio.nextElementSibling.classList.remove('btn-outline-success');        // Les valeurs sont déjà initialisées par le template Twig

            creditRadio.nextElementSibling.classList.add('btn-success');        // Pas besoin de les réinitialiser ici

            debitRadio.nextElementSibling.classList.remove('btn-danger');        

            debitRadio.nextElementSibling.classList.add('btn-outline-danger');        // Fonction pour mettre à jour le montant caché

        } else {        function updateMontantEdit() {

            montantHelp.innerHTML = '<i class="fas fa-arrow-up text-danger me-1"></i>DÉBIT : Le montant sera enregistré comme <strong>négatif</strong> (sortie d\'argent)';            const displayValue = parseFloat(montantDisplayEdit.value) || 0;

            debitRadio.nextElementSibling.classList.remove('btn-outline-danger');            const finalValue = debitRadioEdit.checked ? -Math.abs(displayValue) : Math.abs(displayValue);

            debitRadio.nextElementSibling.classList.add('btn-danger');            montantHiddenEdit.value = finalValue;

            creditRadio.nextElementSibling.classList.remove('btn-success');            

            creditRadio.nextElementSibling.classList.add('btn-outline-success');            // Mettre à jour le style des boutons

        }            if (creditRadioEdit.checked) {

                        creditRadioEdit.nextElementSibling.classList.remove('btn-outline-success');

        // Mettre à jour le champ caché                creditRadioEdit.nextElementSibling.classList.add('btn-success');

        updateRealAmount();                debitRadioEdit.nextElementSibling.classList.remove('btn-danger');

    }                debitRadioEdit.nextElementSibling.classList.add('btn-outline-danger');

            } else {

    function updateRealAmount() {                debitRadioEdit.nextElementSibling.classList.remove('btn-outline-danger');

        const displayValue = parseFloat(montantDisplay.value) || 0;                debitRadioEdit.nextElementSibling.classList.add('btn-danger');

        const isDebit = debitRadio.checked;                creditRadioEdit.nextElementSibling.classList.remove('btn-success');

        const finalAmount = isDebit ? -Math.abs(displayValue) : Math.abs(displayValue);                creditRadioEdit.nextElementSibling.classList.add('btn-outline-success');

        montantReal.value = finalAmount;            }

    }        }

        

    // Événements        // Événements

    creditRadio.addEventListener('change', updateMontantDisplay);        montantDisplayEdit.addEventListener('input', updateMontantEdit);

    debitRadio.addEventListener('change', updateMontantDisplay);        debitRadioEdit.addEventListener('change', updateMontantEdit);

    montantDisplay.addEventListener('input', updateRealAmount);        creditRadioEdit.addEventListener('change', updateMontantEdit);

            

    // Initialiser        // Initialisation

    updateMontantDisplay();        updateMontantEdit();

    }

    // Au moment de la soumission});

    form.addEventListener('submit', function(event) {

        updateRealAmount();// Validation Bootstrap

        console.log('Soumission édition - Montant final:', montantReal.value);(function() {

    });    'use strict';

});    window.addEventListener('load', function() {

</script>        var forms = document.getElementsByClassName('needs-validation');

        var validation = Array.prototype.filter.call(forms, function(form) {

{% endblock %}            form.addEventListener('submit', function(event) {
                if (form.checkValidity() === false) {
                    event.preventDefault();
                    event.stopPropagation();
                }
                form.classList.add('was-validated');
            }, false);
        });
    }, false);
})();
</script>
{% endblock %}