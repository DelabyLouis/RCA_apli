<form method="post" action="{{ path('app_transaction_delete', {'id_transaction': transaction.id_transaction}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transaction.id_transaction) }}">
    <button class="btn">Delete</button>
</form>
